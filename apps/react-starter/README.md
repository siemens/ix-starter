# `@siemens/ix-react` + React + Vite

## Development

Execute `pnpm dev`

## Build

Execute `pnpm build`

## Testing

### Unit tests (vitest + @testing-library/react)

Execute `pnpm test`

### E2E tests (playwright)

Execute `pnpm e2e`
