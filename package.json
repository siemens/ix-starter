{
  "name": "ix-starter",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:create": "pnpm --filter create-siemens-web build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:format": "prettier --config prettier.config.mjs --check .",
    "test": "turbo run test",
    "test:create": "pnpm --filter create-siemens-web build && vitest run",
    "e2e": "turbo run e2e",
    "format": "prettier --config prettier.config.mjs --write ."
  },
  "devDependencies": {
    "@siemens/prettier-config": "^4.4.2",
    "eslint": "^9.12.0",
    "execa": "9.6.1",
    "ix-theme-downloader": "workspace:*",
    "prettier": "3.9.6",
    "turbo": "latest",
    "vitest": "^4.1.10"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "pnpm": ">=10.x.x"
  },
  "volta": {
    "node": "22.21.0"
  }
}
